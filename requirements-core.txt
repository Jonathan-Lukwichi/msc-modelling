# Core dependencies — required to run the full modelling pipeline
# (scripts 01-31), the test suite, and the cross-validation audit.
#
# Install with: pip install -r requirements-core.txt
# This is what the Dockerfile installs by default.

numpy>=2.0
pandas>=2.0
pyarrow>=15
scikit-learn>=1.5
statsmodels>=0.14
pmdarima>=2.0
xgboost>=2.0
optuna>=3.5
pyyaml
matplotlib
seaborn
joblib
tqdm
shap
pytest
scipy>=1.13

# PyTorch CPU build — required directly by src/forecasting/models/ann.py
# and lstm.py. Not previously listed as a direct dependency (was only
# pulled transitively through the optional gluonts[torch] extra), which
# meant a clean venv could run scripts/06 (XGBoost) and scripts/02-04
# (parametric models) but not scripts/07 (ANN) or scripts/08 (LSTM).
torch>=2.2

# Adaptive Conformal Inference (Gibbs & Candes 2021 / Zaffran et al. 2022)
mapie>=0.8

# Kernel mean matching QP solver (Gretton et al. 2009)
cvxopt>=1.3

# Relative density-ratio estimation / RuLSIF (Yamada et al. 2013)
densratio>=0.3

# PDF text extraction for the literature-corpus analysis scripts
PyPDF2>=3.0
