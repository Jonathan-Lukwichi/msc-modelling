# This file is kept for backward compatibility and installs everything
# (core + optional). New setups should prefer the split files:
#
#   pip install -r requirements-core.txt       # pipeline + tests + crossval
#   pip install -r requirements-optional.txt   # DVC/MLflow/Hydra + Nixtla + DeepAR
#
-r requirements-core.txt
-r requirements-optional.txt
