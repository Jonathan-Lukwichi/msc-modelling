numpy>=2.0
pandas>=2.0
pyarrow>=15
scikit-learn>=1.5
statsmodels>=0.14
pmdarima>=2.0
xgboost>=2.0
optuna>=3.5
pyyaml
matplotlib
seaborn
joblib
tqdm
shap
pytest

# Added by Ch6 refactor (Prompt 0). Install on demand per prompt.
# Heavy / optional deps grouped below so a user can pip-install selectively.

# Hydra config + experiment tracking (Prompt 3)
hydra-core>=1.3
dvc[s3]>=3.0
mlflow>=2.9

# Uncertainty quantification (Prompt 8)
mapie>=0.8

# Nixtla ecosystem: direct multi-output XGBoost (Prompt 6), MinT
# reconciliation (Prompt 9), classical statistical baselines.
mlforecast>=0.13
hierarchicalforecast>=0.4
statsforecast>=1.7

# DeepAR multi-task (Prompt 10) — heavy dependency, ~1.5 GB.
gluonts[torch]>=0.14

# Importance-weighted CV via RuLSIF (Prompt 7)
densratio>=0.3
