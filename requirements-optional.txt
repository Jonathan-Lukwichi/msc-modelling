# Optional / heavy dependencies — install on demand.
# Not required for the headline pipeline (scripts 01-31), the test
# suite, or the cross-validation audit. Each block names the feature
# it unlocks so you only pay for what you use.
#
# Install with: pip install -r requirements-optional.txt
# Or per-block, e.g.: pip install "hydra-core>=1.3" "mlflow>=2.9"

# --- Reproducibility platform: config management + experiment tracking ---
hydra-core>=1.3
dvc[s3]>=3.0
mlflow>=2.9

# --- Nixtla ecosystem: direct multi-output XGBoost, MinT hierarchical
#     reconciliation, classical statistical baselines (not yet wired
#     into the executed pipeline; hooks only) ---
mlforecast>=0.13
hierarchicalforecast>=0.4
statsforecast>=1.7

# --- DeepAR multi-task with negative-binomial output — heavy dependency,
#     ~1.5 GB with CUDA extras. Future-work item (chap8 sec 8.7). ---
gluonts[torch]>=0.14
